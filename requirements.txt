# --- Core AI Orchestration ---
langchain>=1.2.3
langchain-community>=0.4.1
langchain-core>=1.2.7
langgraph>=1.0.6
pydantic>=2.12.5

# --- Model Providers ---
# Google / Vertex AI
langchain-google-vertexai>=2.1.2
langchain-google-genai>=2.0.0
google-cloud-aiplatform>=1.133.0
google-genai>=1.57.0

# OpenAI
langchain-openai>=1.1.7
openai>=2.15.0

# --- MCP & Tooling ---
fastmcp>=2.14.3
# Capped below 2.0 deliberately. mcp 2.x removed RequestContext from
# mcp.shared.context, and both fastmcp and langchain-mcp-adapters import it —
# so an unbounded requirement turned every CI run red overnight without a line
# of this project's code changing. Raise the cap only alongside versions of
# those two that support 2.x.
mcp>=1.25.0,<2
# langchain-mcp>=0.2.1
langchain-mcp-adapters>=0.2.1
ddgs>=7.0.0

# --- Vector DB & Data ---
# sqlglot parses the SQL the chat agent writes so it can be validated before it
# reaches the database (backend/sql_guard.py).
sqlglot>=27.0.0
# langchain-postgres was dropped in migration 026. Vectors live in `embedding`
# columns on Transaction and TransactionDetail, queried through SQL functions
# (match_transactions / match_purchase_history), so no vector-store library is
# involved. langchain-core is still used for the Embeddings interface, and
# langchain/langgraph for the agent itself.
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.45
pandas>=2.3.3
redis>=7.1.0

# --- Infrastructure & API ---
uvicorn>=0.40.0
starlette>=0.51.0
python-dotenv>=1.2.1
httpx>=0.28.1
requests>=2.32.5
tenacity>=9.1.2

# Fernet, for encrypting stored API keys at rest (backend/crypto.py). Pinned
# explicitly rather than relied on as a transitive dependency of supabase.
cryptography>=43.0.0


supabase>=2.28.0
plotly>=6.5.2

psycopg2-binary>=2.9.11

# Re-encoding stored receipt photos (scripts/backfill_compress_images.py)
Pillow>=11.0.0



