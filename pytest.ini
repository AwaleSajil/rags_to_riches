[pytest]
testpaths = tests
# conftest.py lives in tests/ and is imported directly by the test modules
# (for the shared user-id constants), so that directory needs to be importable.
pythonpath = . tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
